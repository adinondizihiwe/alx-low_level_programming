#include <stdio.h>

/**
 * main - Entry point
 * Desciption: Print the numbers 0 to 9 on one line
 * Return: 0
 */
int main(void)
{
char i;

for (i = '0'; i<= '0'; ++i)
putchar(i);

putchar('\n');

return (0);
}
