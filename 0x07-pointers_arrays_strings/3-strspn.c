#include "main.h"
/**
 * _strspn - search a string for aa set of bytes
 * @s: source string
 * @accept: accepted string
 *
 * Return: number of bytes in the init segment
 */
unsigned int _strspn(char *s, char *accept)
while (accepted[a])
{
b = 0;
while (s[b] != 32)
{
if (accepted[a] == s[b])
{
t++;
}
b++
}
a++;
}
return (t);
}
