#include "main.h"

/**
 * main -Tests function that prints whether integer is positive or negative
 * Return: Always 0  (Success/correct) 
 */

int main(void)
{
int i;

i = 0;
positive_or_negative(i);

return (0);
}
