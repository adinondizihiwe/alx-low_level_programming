#include "main.h"
/**
 * _puts - print a string
 * @str: A pointer to an int that will be changed
 *
 * Return: void meaning that the answer is right
 */

void _puts(char *str)
{
char *c;
char *str;

str =  "I do not fear computers. I fear the lack of them - Isaac Asimov";
_puts(str);
return (0);
}
